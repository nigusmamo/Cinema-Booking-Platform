table:
  name: halls
  schema: public
array_relationships:
  - name: schedules
    using:
      foreign_key_constraint_on:
        column: hall_id
        table:
          name: schedules
          schema: public
  - name: seats
    using:
      foreign_key_constraint_on:
        column: hall_id
        table:
          name: seats
          schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - name
        - id
      filter: {}
    comment: ""
